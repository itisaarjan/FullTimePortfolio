{
  "name": "portfolio-contact-lambda",
  "version": "1.0.0",
  "description": "Lambda function for portfolio contact form",
  "main": "contact.js",
  "dependencies": {
    "nodemailer": "^6.9.7"
  },
  "scripts": {
    "build": "zip -r contact-lambda.zip . -x '*.zip' 'node_modules/.cache/*'",
    "deploy": "npm run build && aws lambda update-function-code --function-name portfolio-contact --zip-file fileb://contact-lambda.zip"
  }
}
